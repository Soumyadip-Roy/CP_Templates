for(int i=0;i<=n;i++)cout<<vis[i];
            // for(int i = 1 ; i <=n; i++){
            //     int x;cin>>x;

            //     graph[x].push_back(i);
            //     graph[i].push_back(x);
            // }
